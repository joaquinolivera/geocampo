/**
 * Re-export from canonical location.
 * Canonical: src/components/MapCanvas.tsx (accessible via @/components/MapCanvas)
 */
export { default, PastureData, HerdData, MapCanvasProps } from '@/components/MapCanvas';
